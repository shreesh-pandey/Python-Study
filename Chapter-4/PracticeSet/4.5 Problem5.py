# Q.5: Write a program to count the number of zeros in the following tuple: a = (7, 0, 8, 0, 0, 9)

# We have this tuple.
a = (7, 0, 8, 0, 0, 9)

# Lets use count() function to count the number of zeros.
num = a.count(0)

# Lets print the value.
print(num)